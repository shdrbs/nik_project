<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nik.project.mapper.ReplyMapper">

	<!-- 댓글등록 -->
  	<insert id="registReply">
  		insert into product_reply(replyId, id, memberId, content, rating)
  		values(reply_seq.nextval, #{id}, #{memberId}, #{content}, #{rating})
  	</insert>

  	<!-- 댓글 존재 체크 -->
  	<select id="checkReply" resultType="integer">
		select replyId from product_reply
		where memberId = #{memberId} and id = #{id}
	</select>

	<!-- 댓글 페이징 -->
  	<select id="getReplyList" resultType="com.nik.project.model.ReplyDTO">

  		<![CDATA[
		select * from(
			select rownum as rn, replyId, id, memberId, content, rating, regDate
			from product_reply
			where rownum <= #{pageNum} * #{amount}  and id = #{id}
			order by regDate desc
  			)
  		where rn > (#{pageNum} -1) * #{amount}
  		]]>

  	</select>

	<!-- 댓글 총 갯수(페이징) -->
  	<select id="getReplyTotal" resultType="int">
  		select count(*) from product_reply where id = #{id}
  	</select>

  	<!-- 댓글수정 -->
  	<update id="updateReply">
  		update product_reply set content = #{content}, rating = #{rating}
  		where replyId = #{replyId}
  	</update>

	<!-- 댓글 한개 정보(수정페이지) -->
  	<select id="getUpdateReply" resultType="com.nik.project.model.ReplyDTO">
		select * from product_reply where replyId = #{replyId}
	</select>

	<!-- 댓글 삭제 -->
  	<delete id="deleteReply">
  		DELETE FROM product_reply WHERE replyId = #{replyId}
  	</delete>

	<!-- 리뷰 평점 평균 -->
  	<select id="getRatingAverage" resultType="double">
  		select avg(rating) from product_reply where id = #{id}
  	</select>

	<!-- 상품 평점 평균 업데이트 -->
	<update id="updateRating">
		update product set ratingAvg = #{ratingAvg} where id = #{id}
	</update>

</mapper>